# F-Droid repo secrets and generated files — keep the source config, never the built index.
config.yml
config.local.yml
keystore.p12
keystorepass
keypass
archive/
repo/*.apk
repo/*.jar
repo/*.json
repo/*.xml
repo/*.css
repo/*.html
repo/*.png
repo/diff/
repo/status/
repo/icons/
repo/icons-*/
tmp/
srclibs/
stats/
keystore.p12
keystorepass
keypass
