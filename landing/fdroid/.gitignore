# F-Droid repo secrets and generated working directories.
config.yml
config.local.yml
keystore.p12
keystorepass
keypass
archive/
tmp/
srclibs/
stats/

# Allow the F-Droid repo index and APK to be served from Vercel.
!repo/
!repo/**/
!repo/*.apk
!repo/**/*.apk
